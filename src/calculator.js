
function addZero(g,h){
        return g + h;
    }
function addNeg(c,d){
        return c + d;
    };
function add(a,b){
        return a + b;
    };
function addMultiple(){
        var sum=0;
        for(let x=0; x<arguments.length; x++){
            sum +=arguments[x];
        }
        return sum;
    };
function mutliply(i,j){
        return i * j;
    };
function multiplyMul(){
        var product=1;
        for(let x=0; x<arguments.length; x++){
            product *= arguments[x];
        }
        return product;
    };


























